RAILS_MASTER_KEY=$(cat config/credentials/production.key)

# 同じ値を2つの名前で渡す。名前が違うのは受け取る側の都合で、
# 食い違うとアプリだけが認証に落ちる
OKURIBON_DATABASE_PASSWORD=$OKURIBON_DATABASE_PASSWORD
POSTGRES_PASSWORD=$OKURIBON_DATABASE_PASSWORD
