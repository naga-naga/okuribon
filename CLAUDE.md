# おくりぼん（okuribon）

読書交換会ツール。参加者が本を持ち寄り、希望順にもとづくマッチングで交換する。
顔見知りの少人数グループで使うことを想定している。

サービス名は画面と文書ではかな、識別子ではローマ字で書く（`docs/spec.md` 1.）。
名前を書く場所は `config/locales/ja.yml` の `service.name` に絞ってある。
ビューに直接書かない。

## 仕様

`docs/spec.md` が唯一の仕様書。実装の判断に迷ったらここを見ること。
spec.md に書くのは規範だけで、判断の経緯と却下した代替案は
`docs/decisions/`（1件1ファイル）に積む。書式は `docs/decisions/README.md`。
以下は特に厳守する。

### フェーズ

交換会は「準備中 / 登録期間 / 希望提出期間 / マッチング実行待ち / 結果公開」の5つのフェーズを持つ。
フェーズは日時カラムから導出する。**状態カラムは持たない。**

書き込み系の操作は、必ずサーバー側でフェーズを検証すること。
クライアント側の時刻計算に依存しない。

### ギフトコードの可視性

ギフトコードが見えるのは次の2者のみ。

- その本を登録した本人（常時）
- マッチングが成立し、その本を受け取った人

主催者に特権はない。一覧系のアクションやビューには絶対に含めない。
取得経路を1つに限定し、権限判定をそこに集約すること。

### マッチング

- 乱数シードは交換会の作成時に固定する（実行時に生成しない）
- マッチングは一度だけ実行でき、再実行できない
- 実行は行ロックとトランザクションで包む

## 技術構成

- Rails 8 / Ruby 3.4 / PostgreSQL 17
- Hotwire（Turbo + Stimulus）。SPA にはしない
- テストは RSpec。minitest は使わない
- 認証は OmniAuth
- ギフトコードは Active Record Encryption で暗号化する
- 非同期処理と定期実行は Solid Queue（`SOLID_QUEUE_IN_PUMA=true`）
- 通知は Discord / Slack の Webhook。個人宛の通知はない
- デプロイは Kamal

## ビジュアルガイド

画面の見た目は Claude Design のビジュアルガイドに描かれている。
**画面を作る・変える作業は、実装を書き始める前に該当ファイルを開く。**
どのファイルがどの画面かと、その読み方は `docs/spec.md` 10.2 にある。

配色と書体は `app/assets/tailwind/application.css` の `@theme` にトークンとして入っているため、
**ガイドを見なくても色と書体だけは揃ってしまう。** レイアウトと文言の食い違いには気付けない。

資料と食い違ったときは次の順で採る。

1. issue の完了条件。そのまま受け入れ基準になる
2. `docs/spec.md`。唯一の仕様書
3. ビジュアルガイド。資料であって仕様ではない

1 と 3 が両立するなら両方入れてよい。

**ガイドを採らなかったら、その画面の記録に1項目積む。**
積み先は画面ごとに1ファイルで、一覧は `docs/spec.md` 10.2 にある。
項目の書き方は `docs/decisions/README.md`。

MCP が使えずガイドを開けなかったときは、そのまま進めない。
開けなかったことを報告し、PR 本文にも書く。参照できていない実装が、
参照済みのものと同じ顔で main に入るのがいちばん困る。

## 規約

- タイムゾーンは JST（`config.time_zone = "Tokyo"`）
- 現在時刻は `Time.current` を使う。`Time.now` と `Date.today` は使わない（rubocop が弾く）
- 時刻で結果が変わるメソッドは、基準時刻をキーワード引数で必須にする。既定値は置かない。
  現在時刻は入口で1回だけ読み、以降は回す。リクエストでは `ApplicationController#requested_at`。
  既定値を置くと呼ぶたびに時刻が進み、締切をまたいだ瞬間に1つの画面の中でフェーズが食い違う。
  グローバルな時刻の差し替えは持たない。境界を spec で突けるようにするための規約でもある
- ビジネスロジックはモデルとサービスに寄せ、コントローラは薄く保つ
- **同じ判定は1か所に集約し、呼ぶ側それぞれに書かせない。** 可視性・可否・出し分けの条件を
  呼ぶ側ごとに書き写すと、片方だけ直したときに、押しても断られる導線や
  見えてはいけないものが見える経路が残る
- NOT NULL のカラムは presence でも検証する。DB の例外ではなくフォームのエラーとして返すため
- **他のクラスは完全な名前で参照する。** 同じ名前空間の兄弟でも省略しない。
  `Notifications::PhaseChange` の中からでも `Notifications::DeliverJob` と書き、`DeliverJob` とは書かない。
  省略すると、その定数がどこに属するのかをファイルの置き場所から辿ることになる。
  `Matching::Assignment`（構造体）と `Assignment`（レコード）のように、
  短い名前が別のクラスと衝突していることもある。
  自分のクラスの中で使う入れ子の定数（`Exchange::PhaseViolation` など）は、定義がその場にあるので省略してよい
- **ジョブは動詞、サービスは名詞で名前を付ける。** ジョブの入口は `perform` 1つきりで、
  クラスが呼び出し1つに対応する。サービスは複数のメソッドを持つ入れ物なので名詞のままにする。
  品詞そのものが、入口が1つか複数かの目印になる。
  名詞のジョブは何をするのかを名乗らない。`Notifications::PhaseChangeJob` は
  「フェーズを変えるジョブ」とも読めてしまうが、フェーズは日時から導出されるので何も変えていない
- **同じことを受け持つ範囲だけ変えて行うジョブは、同じ動詞に `All` を挟んで対にする。**
  `Notifications::NotifyPhaseChangeJob`（1件・予約）と
  `Notifications::NotifyAllPhaseChangesJob`（全件・走査）。
  やり方（`Scan` など）を名前に出すと、別のことをする2つに見える。
  この2つは同じ判定を通り、受け持つ範囲だけが違う
- 目的語に名前空間の語を重ねない。`Notifications::DeliverJob` であって
  `Notifications::DeliverNotificationJob` ではない。何を配信するかは名前空間が言っている
- コメントとコミットメッセージは日本語で書く

## 検証

変更したら次を通す。落ちたまま完了と報告しない。

- `bin/rspec` — RSpec。作業中は関連ファイルだけでよいが、最後に全体を通す
- `bin/rubocop` — スタイル。`-a` で自動修正できる
- `bin/ci` — rubocop、bundler-audit、importmap audit、brakeman を順に通す

実行は binstub（`bin/`）に揃える。
`bin/rubocop` は `.rubocop.yml` を明示的に渡すため `bundle exec rubocop` と挙動が異なる。
`bin/rspec` は `bundle exec rspec` と実質同じだが、呼び分けを増やさないために揃える。

`bin/ci` にテストは含まれていないので、`bin/rspec` は別に実行する。

DB を伴う rake タスクは `RAILS_ENV` を明示する。
`DATABASE_URL` は現在の環境の設定にしかマージされないため、`RAILS_ENV=test` を付けないと test の接続先が解決されない。

## issue とブランチ

タスクは GitHub issue で管理する。ラベルは2種類。

- `type:epic` — マイルストーンのトラッキング issue。
  task issue を sub-issues として持ち、進捗は GitHub が自動で集計する。
  本文は「目的 / 完了の定義 / 前提 / 参考」の4節
- `type:task` — 1 PR に収まる実装タスク。本文の「完了条件」がそのまま受け入れ基準になる

**Epic 間の依存は Epic 本文の「前提」にしか書かれていない。** task issue だけを読んで着手しない。
task の番号順が着手順に対応する。次に何を取るかは `next` skill で判断する。

main は保護されている。直接コミットせず、ブランチを切って PR を出す。

- ブランチ名は `<issue番号>-<英小文字ケバブの要約>`（例：`12-omniauth`）
- 実装タスクは必ず issue を立ててから着手する。PR 本文に `closes #<issue番号>` を書く
- 環境整備や雑務など issue に紐づかない作業は、番号なしの `<英小文字ケバブの要約>` でブランチを切ってよい。
  この場合 `closes` は書かない
- PR のタイトルと本文は日本語で書く
- PR は merge commit でマージする。squash しない

## コメント

日本語で書く。その場のコードから読み取れない「なぜ」だけを書く。
残すのは、その箇所に固有の理由。

- 却下した代替案と、却下した理由
- そうしないと起きる壊れ方
- コードに現れない外部の制約（ライブラリの挙動、環境の事情など）

次のものは書かない。見つけたら落とす。

- 名前や式の言い換え。クラス冒頭の要約もこれにあたる
- `docs/spec.md` の節番号への参照。節構成が変わると参照は気付かれないまま古くなる
- issue 番号への参照。経緯はコミットと PR が持っている
- リポジトリ全体の規約や仕様の書き写し。規約は `CLAUDE.md` が、仕様は `docs/spec.md` が持つ。
  写しは元を直したときに一斉に古くなる
- spec では、`it` や `describe` の文言の言い換え

### 繰り返される理由

同じ理由が複数の箇所に現れたら、それは箇所に固有の理由ではなく設計方針である。
固有名詞を入れ替えるだけで他の箇所でも通用する文は、これにあたる。
利用者から見える振る舞いの方針（404 で実在を伏せる、押しても断られる導線を残さない、
ギフトコードの可視性など）は `docs/spec.md` が、コードの書き方の方針（判定の集約など）は
`CLAUDE.md`「規約」が一度だけ持つ。持ち場にまだ無い方針は、コメントを落とす前に持ち場へ書き足す。

各箇所のコメントに残すのは、方針をその場に当てはめた事実だけ。

- 何と何を同じ判定から引いているか
- どの経路がそれを受け持つか
- どの箇所が方針の例外にあたり、なぜ例外でよいか

事実だけなら1〜2行に収まる。方針そのものの正しさ（403 だと実在が漏れる、など）を
言い足しはじめたら、それは写しになっている。

spec のコメントは、そのテストの組み立てに固有のことに限る。
なぜこのデータを用意するのか、なぜこの突き方で検証するのか。
テストが守る方針の理由は書かない。振る舞いは `it` の文言が言い、理由は持ち場が言う。

ビューの解説も同じ基準で見る。`docs/spec.md` の同じ画面の規範と重なる部分は落とし、
規範を満たすための実装上の事情（実測した幅、CSS の制約など）だけを残す。

### 言い回し

コメント・文書・画面の文言に共通。一般の技術文書で通じる言い回しだけを使い、
このリポジトリの中でしか通じない比喩を作らない。

- 直す例：リンクや操作を指す「口」、取りこぼしを拾う定期走査を指す「網」、
  クエリが列を「運んでくる」、値や行数が「化ける」、時間の幅を指す「窓」（→ ウィンドウ）
- 残してよい例：「安全側に倒す」（既定を安全な側に取る意）、
  「行き止まり」（遷移先の無い画面の意）など、一般に定着している言い回し
- 「黙って〜する」「静かに〜する」（silently の直訳）は使わない。
  「エラーにならないまま」「気付けないまま」など、何が起きないのかを言う
- 「〜のは〜だけ。」という言い切りの形は使わない。
  「〜だけが〜する」「〜は〜に限る」などの語順に直す
- 「AではなくB」の対比は、Aと誤解する読み手が実際に想定できるときだけ書く。
  Bだけで足りるならAを削る

## コミットメッセージ

日本語で書く。1行目は要約、空行を挟んで本文。

### 1行目

- したことを動詞の終止形で書く（「〜する」）。体言止めにしない
- 全角40字程度まで。超えるなら、1コミットに2つの変更が入っていないか疑う
- `&` や読点で複数の変更をつながない。コミットを分ける
- prefix（`feat:` `fix:` など）は付けない

### 本文

何を変えたかは差分が語る。**なぜそうしたか**を書く。
1行目で言い切れているなら本文は要らない。次に当たるものがあれば書く。

- 却下した代替案と、却下した理由
- 差分に現れない前提（外部ツールの挙動、環境の制約、実際に踏んだエラー）
- その変更が正しいと確かめた根拠
- 同梱した副次的な変更と、同じコミットに入れた理由

全角35字前後で折り返す。
末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` を置く。モデル名はモデルに応じて適宜置き換える。

### 粒度

- 1コミット1目的。こまめにコミットする。spec と実装は同じコミットに入れる
- **各コミット単体で `bin/rspec` が通ること。** 落ちるコミットを挟むと bisect が使えなくなる。
  TDD の Red はコミットしない。Red → Green → Refactor をまとめて1コミットにする
- コミットする前に気付いた粗は、そのコミットに含める。
  typo や rubocop 対応を「あとで直す」コミットに先送りしない
- **push したあとの修正は、履歴を書き換えずコミットを積む。**
  明示的な許可がない限り rebase も amend も force push もしない。
  レビューの指摘に対応したコミットは main に残ってよい。
  どの指摘で何がどう変わったのかを、PR の差分から追えるほうが大事
- リファクタリングが単独で意味を持つ大きさなら、別コミットに分ける
- issue 番号はコミットに書かない。PR 本文の `closes #N` で紐づける

## テスト

spec を伴わない機能追加はしない。TDD で進め、Red → Green → Refactor を1サイクルとして小さく繰り返す。

1. **Red** — 失敗する spec を先に書く。実行し、意図した理由で落ちることを確かめる
2. **Green** — 最短で通す。この段階は汚くてよい
3. **Refactor** — 重複を除き設計を整える。spec が通り続けることを確認しながら進める

- 着手前に TODO リストを作り、1つずつ潰す。複数を並行させない
- Red を確認せずに Green へ進まない。落ちるところを見ていない spec は何も保証しない
- 実装に確信があれば直接書いてよい。無ければ仮実装で通し、例を増やして一般化する
- Refactor 中に spec は変えない。spec を変えるときは Red からやり直す
- テスト対象の振る舞いを spec に書く。実装の内部構造をなぞらない

### request spec と画面の文言

`response.body` の検証は、落ちたときに直すべきものが実装である場合に限る。
文言や見た目を変えるたびに spec の側を書き換えて追従することになる検証は、
たまたまの文言に乗っているだけなので書かない。

検証してよいもの。

- **見えてはいけない値が含まれないこと。** ギフトコードなどは
  `MYOWNGIFTCODE` のような、他と紛れない素の文字列で突く
- **実在を伏せる応答が同一であること。** 404 の本文は応答どうしを突き合わせるか、
  素の文字列で書く。`I18n.t` で組むと、キーが分かれた時点で文面が割れても通ってしまう
- **フェーズ・権限・データの状態による出し分け。** 空状態の言い分けを含む。
  導線はリンクやフォームのパスで検証し、文言で識別するのはパスで見分けられないときに限る
- **利用者が入力した値と、そこから導出される値が出ること。** タイトル・名前・日時・残り時間・冊数など
- **書き込みを拒否した理由やバリデーションのエラーが画面に返ること**
- **仕様書が明記を求めている注記。** ギフトコードの可視範囲など

次のものは検証しない。見つけたら落とす。

- 状態によらず常に出る（または常に出ない）見出し・説明・プレースホルダの固定文言
- 文言の出現回数と、`index` の大小で見る画面部品の並び。
  テンプレートが決めるレイアウトはビジュアルガイドとレビューの持ち場
- 書体・配色などの見た目そのもの

レコードの並びはサーバーが決める挙動なので検証してよい。並びの細則を持つモデルや
サービスがあるなら細かい規則はそちらの spec に置き、request spec は接続の1例に留める。

### factory の既定値

**テストの結果を左右する値は、そのテストで明示する。** factory の既定に任せてよいのは、
結果を左右しない値に限る。名前や URL のように、何が入っていても同じ結果になるものがこれにあたる。

既定に乗ると、そのテストが成り立つ条件がテスト本文から読めない。
「factory の既定は登録期間中」のような断り書きが要るなら、それは明示すべき値である。

値を入れ替えただけの例が隣に並ぶときは、どちらの例でもその値を書く。
片方が既定に乗っていると、2つの例が何で分かれているのかが読めない。
`Assignment#returned` の成立と返却がこれにあたる。

交換会のフェーズは `Exchange::PHASES` と同じ名前の trait で書く（`create(:exchange, :registration)`）。
trait の日時は現在時刻からの相対で置いてあるので、`travel_to` で時刻を固定する spec では
trait ではなく絶対値の日時を渡す。

交換会の factory の既定は準備中にしてある。書き込みは参加しか通らないので、
フェーズに乗ったテストが trait を書き忘れると、通らずに落ちる。

## 作業の進め方

- 大きな変更の前に方針を説明し、合意を取ってから着手する
- issue に着手するときは、その issue と親 Epic、`docs/spec.md` の該当節を先に読む
- スキーマの変更は必ずマイグレーションで行う
- 仕様書にない判断を実装中にしたら、守るべき決まりは `docs/spec.md` に、
  経緯と却下した代替案は `docs/decisions/` に書き足す
